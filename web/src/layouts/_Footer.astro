---

---

<footer class="footer md:footer-horizontal bg-base-100 text-base-content p-10">
  <nav>
    <!-- <h6 class="footer-title">Services</h6>
    <a href="/products/forge" class="link link-hover">IT End2End Forge</a>
    <a href="https://gold-swan.is" class="link link-hover"
      >Gold Swan ALL-IN Agency</a
    > -->
  </nav>
  <nav>
    <h6 class="footer-title">Company & Contacts</h6>
    <!-- <a href="/about" class="hidden link link-hover">About us</a> -->
    <!-- <a target="_blank" href="https://t.me/growplex" class="link link-hover"
      >Manager's Telegram</a
    > -->
    <a
      target="_blank"
      href="mailto:cogitoteamdev@gmail.com"
      class="link link-hover">cogitoteamdev@gmail.com</a
    >
  </nav>
  <nav>
    <h6 class="footer-title">Legal</h6>
    <a href="/terms-and-privacy" class="link link-hover">Terms & Privacy</a>
  </nav>

  <form
    method="post"
    action="https://listmonk.cogitosoftware.nl/subscription/form"
    class="listmonk-form w-full max-w-xs"
    id="subscribe-form"
  >
    <div class="flex flex-col gap-3 p-4 rounded-xl bg-base-200 shadow-lg">
      <h3 class="footer-title text-lg font-bold mb-2 flex items-center gap-2">
        <span class="text-yellow-400">🌼</span> Subscribe
      </h3>
      <input type="hidden" name="nonce" />

      <label class="form-control w-full">
        <span class="label-text mb-1">E-mail</span>
        <input
          type="email"
          name="email"
          required
          placeholder="E-mail"
          class="input input-bordered w-full"
        />
      </label>

      <label class="form-control w-full">
        <span class="label-text mb-1"
          >Name <span class="text-xs text-base-content/60">(optional)</span
          ></span
        >
        <input
          type="text"
          name="name"
          placeholder="Name (optional)"
          class="input input-bordered w-full"
        />
      </label>

      <!-- Hidden Quizbee Landing toggle -->
      <input
        id="c687c"
        type="checkbox"
        name="l"
        checked
        value="c687ccd4-0f1c-4a6a-af2d-12b7ae9904b4"
        class="hidden"
        style="display:none"
        aria-hidden="true"
        tabindex="-1"
      />

      <button
        type="submit"
        class="btn btn-primary mt-3 flex items-center gap-2"
        id="subscribe-btn"
      >
        <span id="btn-text">Subscribe</span>
        <span class="text-yellow-400">🌼</span>
      </button>
    </div>
  </form>
</footer>

<script>
  const form = document.getElementById("subscribe-form") as HTMLFormElement;
  const btn = document.getElementById("subscribe-btn") as HTMLButtonElement;
  const btnText = document.getElementById("btn-text") as HTMLSpanElement;

  form?.addEventListener("submit", async (e) => {
    e.preventDefault();
    if (btnText.textContent === "Subscribed!") return;

    const formData = new FormData(form);

    try {
      btn.disabled = true;
      btnText.textContent = "Subscribing...";

      await fetch(form.action, {
        method: "POST",
        body: formData,
      });
    } finally {
      btn.disabled = false;
      btn.classList.remove("btn-primary");
      btn.classList.add("btn-success");
      btnText.textContent = "Subscribed!";
    }
  });
</script>
