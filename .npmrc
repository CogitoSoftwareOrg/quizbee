# pnpm workspace configuration
# Generate per-app lock files instead of single root lock
shared-workspace-lockfile=true

# Enable workspace package linking for hot-reload in dev
link-workspace-packages=true

# Prefer workspace packages over registry in dev
prefer-workspace-packages=true
