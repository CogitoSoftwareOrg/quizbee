import { APIRequestContext } from "@playwright/test";

export async function seedMinimal(api: APIRequestContext) {
  // создайте тестовый набор: категории, курс, квиз
}

export async function cleanup(api: APIRequestContext) {
  // удалите созданные сущности
}
