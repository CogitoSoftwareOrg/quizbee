[project]
name = "quizbee-example-lib"
version = "0.1.0"
description = "Example shared library for QuizBee"
readme = "README.md"
requires-python = ">=3.13"
authors = [
    { name = "QuizBee Team" }
]
license = { text = "MIT" }

# Add dependencies here
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/quizbee_example_lib"]
