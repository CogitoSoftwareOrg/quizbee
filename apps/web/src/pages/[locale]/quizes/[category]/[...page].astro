---
import type { GetStaticPaths, Page } from "astro";
import { getStaticQuizCategoryLocalePaths } from "@/ssg/quizes";
import QuizCategory from "@/components/pages/quizzes/QuizCategory.astro";

export const getStaticPaths = getStaticQuizCategoryLocalePaths;

const { page } = Astro.props as {
  page: Page;
};
---

<QuizCategory page={page} />
